# Release Assurance Mapping

The export includes release decision, finding-level release contribution, matched rule
IDs, required approvals, required actions, decision ID and policy version.

The bundle does not claim approvals were granted unless approval evidence exists in the
repository. The current local evidence exports the release decision as
`conditional_pass`.

