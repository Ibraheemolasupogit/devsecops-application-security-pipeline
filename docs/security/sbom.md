# SBOM

SBOM generation targets CycloneDX JSON:

```bash
make sbom
make verify-sbom
```

The deterministic fallback SBOM is generated by `genomic_research_access_api.security.appsec.evidence` and avoids absolute local paths. Verification checks CycloneDX shape and expected project component presence.
