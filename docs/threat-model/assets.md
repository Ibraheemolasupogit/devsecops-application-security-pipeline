# Assets

The authoritative asset register is `assets.yaml`.

Key assets include the synthetic dataset catalogue, dataset metadata, access requests, approval decisions, audit events, application configuration, source code, API schemas, repository state, correlation identifiers and future cloud/security artefacts.

Synthetic data is not classified as real genomic or patient data. Workflow and audit records are still treated as sensitive because their integrity matters to the operating model.
