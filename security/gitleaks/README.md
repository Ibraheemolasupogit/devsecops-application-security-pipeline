# Gitleaks Configuration

The committed RSA private key under `tests/fixtures/keys/dev_private_key.pem` is synthetic Milestone 3 local test material. The allow-list is scoped to that exact path only and is tracked in `security/config/suppressions.yaml` with owner, reason, review date and expiry.

Private-key detection is not disabled globally.
