# Trust Boundaries

The authoritative trust-boundary register is `trust-boundaries.yaml`.

The model includes current local boundaries between clients, API routing, schemas, service layer, repositories and audit store. It also includes future boundaries for CI/CD to cloud, runtime to datastore, public network to private components and workload identity to secrets or encryption services.
