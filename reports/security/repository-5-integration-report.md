# Repository 5 Integration Contract

Contract: `product-security-control-plane-export` version `1.0`.

A consumer control plane can validate and ingest the bundle by verifying the manifest, checksums, schemas, record counts and data-handling constraints.

Producer repository: `devsecops-application-security-pipeline`.
Deployment status: `not_deployed`.
No Repository 5 files are modified and no external transfer is performed.
