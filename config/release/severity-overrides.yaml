{
  "schema_version": "1.0",
  "overrides": [
    {
      "name": "suppression does not lower severity",
      "condition": "suppression_status == active",
      "effect": "preserve_source_severity"
    }
  ]
}
