# Entry Points

The authoritative entry-point register is `entry-points.yaml`.

Current entry points include HTTP endpoints, request payloads, path parameters, headers, correlation ID header, application configuration, environment variables, container image, dependency installation and CI/CD workflow definitions.

Future entry points include Terraform inputs, cloud IAM trust policies and logging integrations.
